# Hull Tactical Market Prediction — PMLDL 2026 Stage 2
# Python 3.11+ (verified on 3.12)
#
# Versions are pinned to the set this project was developed and executed
# against. Install with:
#     pip install -r requirements.txt

# --- core ---
numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
pyarrow==22.0.0         # hard dependency of pandas 3.x (pa.Table backend)
polars==1.44.2          # the baseline notebooks read/serve rows as polars frames

# --- modelling ---
scikit-learn==1.8.0
lightgbm==4.7.0
xgboost==3.0.5          # baseline 03 (100th place ensemble)
catboost==1.2.10        # stage 3 ensemble
optuna==5.0.0
joblib==1.5.3

# --- plotting / EDA (used by the converted baseline notebooks) ---
matplotlib==3.10.8
seaborn==0.13.2
plotly==7.0.0
tabulate==0.9.0         # DataFrame.to_markdown() in the baseline 02 EDA helpers

# --- notebook runtime ---
jupyterlab==4.4.9
ipykernel==6.30.1
ipywidgets==8.1.7       # Optuna progress bars