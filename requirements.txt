# Hull Tactical Market Prediction - PMLDL 2026, Stage 2
# Exact versions used to produce the committed notebook outputs (Python 3.12).
# Install with:  pip install -r requirements.txt

numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
scikit-learn==1.8.0
matplotlib==3.10.8

lightgbm==4.7.0
xgboost==3.4.1
catboost==1.2.10
optuna==5.0.0

# notebook execution / authoring
nbformat==5.11.1
nbconvert==7.16.6
ipykernel==7.1.0
jupyterlab==4.5.4

# --- known-compatible lower bounds -------------------------------------------------
# The code does not rely on version-specific behaviour; if the pins above conflict with
# your environment, these minimums are sufficient:
#   numpy>=1.26  pandas>=2.1  scipy>=1.11  scikit-learn>=1.3  matplotlib>=3.7
#   lightgbm>=4.0  xgboost>=2.0  catboost>=1.2  optuna>=3.4
